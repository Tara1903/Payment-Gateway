/**
 * @starpay/sdk
 *
 * StarPay JavaScript SDK
 * Stub — reserved for future merchant integration use.
 *
 * Will expose:
 * - StarPayClient: initialize with merchantId + apiKey
 * - createOrder(): create and return a hosted payment link
 * - getOrderStatus(): poll payment status
 * - webhookVerify(): verify StarPay webhook signatures on merchant server
 */

export const SDK_VERSION = '0.1.0';

// Future exports will be added here as the SDK is built.
